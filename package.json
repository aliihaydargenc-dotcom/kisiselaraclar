{
  "name": "kisiselaraclar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/main.js && node --check src/catalog.js && node --check src/tool-engines.js",
    "test": "node --test tests/*.test.mjs",
    "quality": "npm run check && npm test"
  }
}
