{
  "name": "kisiselaraclar",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check src/main.js && node --check src/catalog.js && node --check src/tool-engines.js && node --check src/integrations.js && node --check src/pdf-tools.js && node --check src/pdf-ui.js",
    "test": "node --test tests/*.test.mjs",
    "size": "node scripts/check-build-size.mjs",
    "quality": "npm run check && npm test && npm run build && npm run size"
  },
  "dependencies": {
    "papaparse": "5.7.0",
    "pdf-lib": "1.17.1",
    "pdfjs-dist": "6.3.289"
  },
  "devDependencies": {
    "vite": "8.3.0"
  }
}
